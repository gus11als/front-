//상수는 변하지않는 값으로 값을 정의하면 변경이 불가하다 또한 선언과 초기화를 따로할수가 없다\

const min = 10;
console.log(min);
// min = 50;
// console.log(min);  변경이 불가능


const promptmessage = "프롬프트 다이얼로그에 표시될 문자열"
const result = prompt(promptmessage)
console.log(result);